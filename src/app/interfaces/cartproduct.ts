export interface Cartproduct {
    id: string,
    name: string,
    category: string,
    price: number,
    quantity: number,
    selectedQuantity: number,
    description: string,
    image: string
}
